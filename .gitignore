# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Editors
.vscode/
.idea/
*.swp

# Local screenshots / scratch
/tmp/
*.local

# Server log written by `make`
.taxfix-server.log

# Note: web/data/run.json and web/data/run.js are generated by `node run-demo.mjs`
# but are intentionally committed, so the prototype opens straight from the
# filesystem (or GitHub Pages) with no build step and no server.
